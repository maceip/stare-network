{
  "version": 1,
  "env": [
    "PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin",
    "HOME=/root",
    "TERM=dumb"
  ],
  "examples": {
    "alpine": {
      "image": "friscy-alpine",
      "rootfs": "/friscy-golden/rootfs.tar",
      "entrypoint": ["/bin/sh", "-i"],
      "allowNetwork": true
    },
    "nodejs": {
      "image": "friscy-nodejs",
      "rootfs": "/friscy-golden/nodejs.tar",
      "entrypoint": ["/usr/bin/node", "--jitless", "--max-old-space-size=64", "-e", "const fs=require('fs'),vm=require('vm'),util=require('util');const ctx=vm.createContext({require,console,process,Buffer,globalThis});let line='';process.stdout.write('> ');const b=Buffer.alloc(1);while(true){const n=fs.readSync(0,b,0,1,null);if(n<=0)break;const ch=b.toString('utf8',0,1);if(ch==='\\r')continue;if(ch!=='\\n'){line+=ch;continue;}const src=line;line='';const t=src.trim();if(t===''){process.stdout.write('> ');continue;}if(t==='.exit'||t==='exit'){break;}try{const out=vm.runInContext(src,ctx);if(out!==undefined)process.stdout.write(util.inspect(out)+'\\n');}catch(e){process.stdout.write(String((e&&e.stack)||e)+'\\n');}process.stdout.write('> ');}"],
      "allowNetwork": true
    },
    "claude-cli": {
      "image": "friscy-nodejs",
      "rootfs": "/friscy-golden/nodejs.tar",
      "entrypoint": ["/usr/bin/node", "--jitless", "--max-old-space-size=64", "-e", "const https=require('https');const d=JSON.stringify({model:'claude-sonnet-4-20250514',max_tokens:120,messages:[{role:'user',content:'Write a short haiku about RISC-V emulation in WebAssembly.'}]});const req=https.request({hostname:'api.anthropic.com',port:443,path:'/v1/messages',method:'POST',headers:{'Content-Type':'application/json','x-api-key':process.env.ANTHROPIC_API_KEY||'', 'anthropic-version':'2023-06-01','Content-Length':Buffer.byteLength(d)}},res=>{let b='';res.on('data',c=>b+=c);res.on('end',()=>{try{const j=JSON.parse(b);if(j&&j.content&&j.content[0]&&j.content[0].text){console.log(j.content[0].text);}else{console.log(b.slice(0,400));}}catch(e){console.log(b.slice(0,400));}process.exit(0);});});req.on('error',e=>{console.log('Error:',e.message);process.exit(1);});req.write(d);req.end();setTimeout(()=>{console.log('Timeout');process.exit(1);},60000);"],
      "env": ["ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}"],
      "allowNetwork": true
    }
  }
}
